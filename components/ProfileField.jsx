import { View, Text, TextInput } from 'react-native'
import React from 'react'

const ProfileField = ({ placeholder, disabled }) => {
    return (
        <TextInput
            placeholder={placeholder}
            className=""
        />
    )
}

export default ProfileField